
 
  
class DataStore:
    def init(file_name):
        self.file_name = file_name
        self.Profiles = {email_id: Profile}
        self.LoadData()

    # This class has to be static/singleton, can only called once per file name.  
    def LoadData():
        # for each ride in the csv :
            # Add Profile, rides whatever you want to database 

    def GetProfile(string email_id): Profile 
        return Profile/Person 

    def GetRider(string ride_id): Ride 

    def AddProfile(Profile profie):

    def AddRide(Ride ride):

